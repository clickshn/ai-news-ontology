# 이 프로젝트에서 내린 판단들

`ai-news-ontology` 를 만들며 실제로 부딪힌 문제와 그때의 판단을 기록한 문서다.
결정의 전체 목록은 [README 결정 로그](README.md#결정-로그-decision-log)(D-001~D-047)에
있고, 여기에는 **설명할 가치가 있는 8건**만 골라 `상황 → 판단 → 근거 → 결과` 로 정리했다.

인용한 숫자는 모두 레포에 기록된 실측값이다. 출처를 각 항목 끝에 달았다.

---

## 1. 게이트가 실행마다 다른 답을 냈다 — 원인은 프롬프트가 아니라 설정이었다

**상황.** 관련성 게이트(Haiku 4.5)가 같은 기사를 5회 실행에서 4번 통과시키고 1번
스킵했다. 첫 가설은 "프롬프트가 애매해서 경계 사례에서 흔들린다"였고, 프롬프트에
"애매하면 통과" 문구를 보강하려던 참이었다.

**판단.** 프롬프트를 고치기 전에 **요청에 실제로 무엇이 실려 나가는지** 먼저 확인했다.
`config.yaml` 에 `temperature` 항목 자체가 없었고, 그래서 API 기본값 1.0 이 적용되고
있었다. 게이트는 "분석할 가치가 있는가"라는 **이분 판정**이라 샘플링 다양성이 필요 없다.

**근거.** 프롬프트를 고쳤다면 증상이 줄었을 수는 있어도 원인은 남는다. 같은 입력이
실행마다 다르게 처리되면 파이프라인 자체를 신뢰할 수 없고, 게이트의 오탐률을 측정할
수도 없다. 반면 정밀 추출(Opus 5)은 temperature 를 400 으로 거부하므로 **같은 설정을
전 단계에 적용할 수 없었다** — 단계별로 파라미터 지원 범위가 다르다는 것을 여기서
처음 확인했고(Haiku 는 `effort` 를 거부, Opus 5 는 `temperature` 를 거부), 그래서
파라미터를 모델별로 끌 수 있게 optional 로 바꿨다.

**결과.** 게이트에만 `temperature: 0` 을 고정한 뒤 5회 재실행에서 판정과 근거 텍스트가
**바이트 단위로 동일**했다. 설정이 사라지는 것을 막기 위해 `config.yaml` 의 값을
검증하는 테스트를 두 개 추가했다(게이트는 0, 추출은 null 이어야 한다).

> D-018 · D-032 · D-033 / `tests/test_vocab_sync.py`

---

## 2. LLM judge 와 사람의 점수가 갈렸다 — 편향이 아니라 서로 다른 것을 세고 있었다

**상황.** 요약 품질을 채점하는 LLM-judge 를 처음 돌렸더니 완결성 축에서 judge 3점,
사람 1점이 나왔다. judge 와 추출기가 같은 모델(Opus 5)이라 **자기 출력에 후한 점수를
주는 자기 편향**이 먼저 의심됐다. 두 번째 가설은 "루브릭 구간이 뭉뚱그려져 있어서
누락이 몇 개든 3점 근처로 수렴한다"였다.

**판단.** 두 가설을 확정하기 전에 **양쪽이 지적한 누락 항목을 나열해 대조**했다.
judge 는 3개(재현 조건·영향 범위·심각도 등급), 사람은 4개(발견 비용·신호·등급·수정안)를
지적했는데 **합집합 6개 중 겹치는 것은 1개뿐**이었다. 관대함의 차이가 아니라
**무엇이 "핵심"인지에 대해 서로 다른 축을 고르고 있었던 것**이다.

**근거.** 이 관측이 해법을 바꿨다. 구간만 촘촘하게 나누면 여전히 서로 다른 대상에
다른 눈금을 대는 것이므로 격차가 남는다. 그래서 루브릭 v3 은 구간 세분화보다 **먼저
셀 대상을 고정**했다 — 핵심 정보를 4슬롯(사건/범위/정도/경위)으로 정의하고, "이름만
언급하고 구체가 없으면 미충족"을 명시한 뒤, 슬롯 충족 개수로 구간을 나눴다.

**결과.** v3 을 같은 사례에 손으로 적용하면 완결성 2점으로, judge(3)와 사람(1)
**사이**에 떨어졌다. 이 시점에는 예상치일 뿐이라 그렇게 명시하고, 첫 실행 결과를
"자기 편향 확인"이 아니라 **기준선**으로만 기록했다.

**검증(2026-09-01).** 예상치를 검증하면서 두 가지를 지켰다. 첫째, **판정 기준을
데이터를 보기 전에 고정**했다(중심 이동 / 흔들림 감소 / 루브릭 준수 3개 가설과
임계값). 실행 후에 정하면 어떤 결과가 나와도 성공으로 읽을 수 있다. 둘째,
**v2 를 대조군으로 함께 돌렸다** — v3 만 반복하면 "v3 가 점수를 옮겼다"와
"judge 가 원래 2~3 을 오가는데 우연히 3 을 뽑았다"를 구분할 수 없어, 표본 1건으로
편향을 주장하지 않겠다던 원칙이 한 층 위에서 그대로 깨진다.

결과는 **v3 8/8 회 2점, v2 5/5 회 3점**으로 예상치가 재현됐다. 더 중요한 것은
점수가 아니라 근거였다 — 8회 모두 **같은 슬롯을 같은 이유로** 판정했다(①충족,
②③④미충족). 점수만 맞았다면 우연일 수 있지만 판정 경로까지 일치하면 아니다.

그래도 **자기 편향은 여전히 재지 못했다.** 사람 점수가 n=1 이고, 그 1점에는
루브릭 밖 기준이 섞여 있다. judge 가 2로 완벽히 수렴한 것은 "사람과 일치"가
아니라 **"내 손계산과 일치"** 이고, 손계산은 내가 v3 를 쓰면서 한 것이라 독립적인
검증이 아니다. 확인된 것은 정확히 "완결성 격차의 원인이 샘플링 흔들림은
아니었다"까지다.

> D-041 · D-042 · D-043 · D-045 · D-048 / `eval/README.md` "v3 검증 실행"

---

## 3. 비싼 모델 앞에 싼 모델을 뒀다 — 다만 게이트에 분류는 시키지 않았다

**상황.** 수집 소스 중 GeekNews 는 개발자 커뮤니티라 AI 무관 글이 섞여 있다. 실측 결과
**최근 50건 중 약 60%만 AI 관련**이었다. 전량을 Opus 5 + effort high 로 넘기면 버려질
결과에 비용의 40% 를 쓰는 셈이다.

**판단.** 정밀 추출 앞에 **Haiku 4.5 관련성 게이트**를 두고, "분석할 가치가 있는가"만
이분 판정하게 했다. 수집 단계에서 키워드로 거르는 대안은 택하지 않았다 — 제목에
AI 관련성이 드러나지 않는 글이 많아 놓치는 게 많고, 판정은 본문을 읽을 수 있는 곳으로
미루는 편이 낫다. **게이트에 분류(온톨로지 라벨링)까지 시키지 않은 것이 핵심이다.**

**근거.** 저비용 모델의 분류 결과가 정밀 추출 결과와 섞이면 **어느 쪽 품질을 재고 있는지
알 수 없어진다.** eval 이 "추출 정확도"를 측정한다고 말하려면 추출을 한 주체가 하나여야
한다. 비용 최적화가 측정 가능성을 깨뜨리지 않도록 역할을 분리했다. 같은 이유로 게이트는
**재현율을 우선**한다 — 애매하면 통과시킨다. 잘못 버린 기사는 영영 사라지지만 잘못
통과시킨 기사는 추출 비용 한 번을 더 쓸 뿐이다.

**결과.** 100건 기준 **$4.29 → $2.92, 약 32% 절감**(게이트 $0.003460/건, 추출
$0.042875/건, 비AI 40% 가정). 손익분기는 스킵률 **8.1%** 로, 관측된 40% 는 그 5배다.
게이트가 버린 항목은 판정 근거와 함께 JSONL 로 남겨 나중에 오탐을 표본 검토할 수 있게
했다 — 스킵이 흔적 없이 사라지면 "재현율 우선" 결정이 지켜지는지 검증할 방법이 없다.

> D-015 · D-016 · D-017 / `observability/README.md` "게이트 비용 절감 실측"

---

## 4. 기업명 정규화에서 퍼지 매칭을 쓰지 않았다 — 오류가 비대칭이기 때문

**상황.** LLM 이 뽑아낸 기업명은 표기가 제각각이다(`오픈AI`, `Open AI`, `OpenAI`).
Obsidian 그래프에서 같은 기업이 여러 노드로 갈라지지 않게 정규화가 필요했다. 편집거리나
부분 문자열 매칭을 쓰면 커버리지가 올라간다.

**판단.** **퍼지 매칭을 쓰지 않았다.** 사전(`config.yaml: company_aliases`)에 명시적으로
등록된 표기만 정규화하고, 흡수하는 차이는 **대소문자와 공백뿐**이다. `Open AI → OpenAI`
는 흡수하지만 `OpenAI Korea` 는 별개 엔티티로 남긴다.

**근거.** 두 방향의 오류가 비대칭이다. 정규화를 **놓치면** `resolved=false` 로 남고
원문 표기가 보존되므로, 나중에 사전에 한 줄 추가하면 복구된다 — 손실은 그래프 노드가
하나 더 생기는 것뿐이다. 정규화를 **잘못하면** 서로 다른 기업이 한 노드로 합쳐지고,
합쳐진 뒤에는 원래 무엇이었는지 알 수 없다. **미탐은 되돌릴 수 있고 오탐은 되돌릴 수
없다.** 그래서 정밀도를 재현율보다 우선했다 — 같은 프로젝트의 관련성 게이트가 정반대로
재현율을 우선하는 것과 대비된다. 두 곳의 오류 비용 구조가 반대이기 때문이다.

**결과.** 미등록 표기는 `unknown_companies.jsonl` 에 **이름당 한 줄로 upsert** 되어
등장 횟수와 함께 쌓인다. 사전 보강 대상을 빈도순으로 고를 수 있는 작업 큐다. 합칠 때는
정규화와 **같은 동치 관계**를 쓴다 — 키가 같은 두 표기는 사전에 한 줄만 넣으면 둘 다
해결되므로 합치는 것이 맞고, 이건 퍼지 매칭이 아니다.

> D-011 · D-025 · D-026 · D-035 · D-036 / `extraction/normalize.py`

---

## 5. 통제어휘를 확장하면서 과거 라벨을 소급 오염시키지 않았다

**상황.** 첫 추출에서 두 가지 어휘 문제가 동시에 나왔다. (a) 커뮤니티 발 버그 리포트가
`발표유형` 에 갈 곳이 없어 `Benchmark/Report` 로 밀렸다. (b) LLM 을 **보조 도구로만**
쓴 글이 `기술영역: Agent` 로 분류됐다.

**판단.** 겉보기에 같은 종류의 문제인데 **다른 층위로 답했다.** (a) 는 어휘에 값을
추가했다(`Community/Discussion`, 6개 → 7개). (b) 는 **어휘를 건드리지 않고** 문서와
프롬프트에서 판단 기준을 좁혔다.

**근거.** 어휘에 값을 **추가**하는 것과 기존 값을 **쪼개는** 것은 소급 효과가 다르다.
값을 추가하면 과거 라벨은 그대로 유효하다. 하지만 `Agent` 를 세분화하면 **이미 쌓인
노트의 라벨이 소급해서 틀린 것이 된다.** 게다가 "무엇을 Agent 로 볼 것인가"는 사례가
쌓이며 계속 바뀌는 종류의 기준이라, 스키마(Enum)가 아니라 문서가 맞는 층위였다.
(a) 를 가르는 기준도 "무엇을"이 아니라 **"누가"** 로 잡았다 — 공식 조직이면 다른 값,
개인·커뮤니티면 `Community/Discussion`.

**결과.** 프롬프트를 v2 로 올려(v1 은 보존) 같은 기사를 재추출했더니 두 규칙이 모두
라벨을 바꿨다 — `Agent` 가 빠지고, `Benchmark/Report` → `Community/Discussion` 으로
이동했다. 어휘 자체는 `schema.py` Enum 이 단일 출처이고, `config.yaml` 미러와의 동기화는
테스트가 강제한다. 프롬프트에는 어휘를 하드코딩하지 않고 런타임 주입한다 — 하드코딩하면
같은 어휘의 사본이 네 곳(코드·설정·문서·프롬프트)이 되고, 프롬프트는 테스트하기 어려워
가장 먼저 썩는다.

> D-002 · D-012 · D-021 · D-022 · D-023 · D-024

---

## 6. "모델 출력은 정답이 아니다"를 주석이 아니라 스키마로 강제했다

**상황.** 골든셋을 만들려면 파이프라인 실행 결과를 옮겨 적는 것이 가장 빠르다. 하지만
모델 출력을 그대로 정답으로 삼으면 eval 이 **모델이 스스로를 채점하는** 구조가 된다.
문서에 "모델 출력은 정답이 아니다"라고 적어 두는 것만으로는 부족했다.

**판단.** 골든셋 항목에 `status: draft | confirmed` 를 두고 **로더가 기본적으로
`confirmed` 만 읽게** 했다. `confirmed` 로 올리는 순간 영향도·본문·라벨러·라벨링
날짜가 모두 있어야 하고, `<<채워주세요>>` 자리가 하나라도 남아 있으면 pydantic
검증에서 걸린다.

**근거.** 초안이 실수로 섞이면 **점수는 높게 나오고 그 사실이 결과 어디에도 드러나지
않는다.** 조용히 틀리는 종류의 오류라 사람의 주의력이 아니라 코드로 막아야 한다. 채점기가
"정답이 없음"과 "정답이 X"를 구분하지 못하면 accuracy 자체가 의미를 잃으므로, 승격 조건도
검증에 넣었다. 같은 이유로 **잴 수 없었던 지표는 0 이 아니라 `null`** 로 남긴다 — judge 를
안 돌렸으면 요약 점수는 `null` 이고 임계값 판정 자체를 하지 않는다.

**결과.** 첫 확정 항목에서 사람은 영향도를 1점으로 매겼고 모델은 세 번의 실행에서 모두
2점을 냈다 — 채점기가 이 불일치를 잡아내 `field_accuracy` 가 0.83 으로 임계값 0.90 을
미달했다. 라벨링 가이드에는 **"영향도에 루브릭 밖 기준을 섞지 않는다"** 를 명문화했다.
직무 관련성은 이미 **관련성 게이트가 판정한 축**이라, 여기서 다시 적용하면 한 축을 두 번
세는 셈이고 eval 이 게이트 성능과 영향도 정확도를 구분하지 못하게 된다. 규칙 이전에
매긴 라벨은 `labeling_guideline` 필드로 표시만 하고 **고치지 않았다** — 근거는 실제 판단
과정의 기록이고, 나중에 만든 규칙에 맞춰 다시 쓰면 그 기록이 거짓이 된다.

> D-039 · D-040 · D-046 · D-047 / `eval/schema.py` · `eval/golden_set/README.md`

---

## 7. 자유 태그 표기 통일에 사전을 쓰지 않고 프롬프트 규칙을 썼다

**상황.** `관련기존기술` 은 자유 태그 필드다. 첫 추출에서 **한 기사 안에서만 표기가 세
갈래로 갈렸다** — `퍼징(Fuzzing)` 병기, `바이브코딩` 순한글, 영어 원형. 이대로 쌓이면
Obsidian 에서 같은 개념이 여러 태그로 흩어져 검색이 안 된다.

**판단.** 기업명과 달리 **사전을 만들지 않고 프롬프트 규칙으로 강제**했다. "영어 표준형을
쓰고 한글을 병기하지 않는다"를 명시하고, 실제로 나왔던 표기를 **반례로** 프롬프트에
넣었다.

**근거.** 기업명은 어휘 규모가 대략 정해져 있지만(주요 AI 기업은 수십 개) 이 필드는
**어휘 규모를 아직 모른다.** 규모를 모르는 채 사전을 먼저 만들면 틀린 사전이 되고,
유지 비용이 개인 프로젝트에서 감당이 안 된다. 생성 시점에 표기를 맞추는 쪽이 싸다.
병기(`퍼징(Fuzzing)`)를 명시적으로 금지한 것은 **그게 세 번째 변종을 만들기** 때문이다.
같은 문제를 필드 성질에 따라 다르게 푼 셈이다 — 닫힌 어휘는 사전, 열린 어휘는 규칙.

**결과.** 같은 기사를 새 프롬프트로 재추출하니 `퍼징(Fuzzing)`/`바이브코딩` 이
`Fuzzing`/`Vibe Coding` 으로 바뀌었다. 사전 없이 프롬프트만으로 표기가 통일되는지
의심스러웠으나 관측된 사례에서는 작동했다. 규칙이 프롬프트에서 조용히 사라지는 것을
막기 위해, 좋은 예와 반례가 실제로 프롬프트 파일에 들어 있는지 검사하는 테스트를 뒀다 —
프롬프트는 코드가 아니라 텍스트라 조용히 썩는다.

> D-020 · D-024 / `tests/test_prompt_policy.py`

---

## 8. 공개 레포에 개인 이메일이 커밋 20건에 남아 있었다

**상황.** 포트폴리오용 공개 레포를 점검하다가, 커밋 메타데이터의 author 이메일이
실제 개인 gmail 주소인 것을 발견했다. 흥미로운 점은 **초기 커밋 1건은 GitHub 의
noreply 주소로 보호돼 있었다**는 것이다.

**판단.** 원인은 GitHub 계정 설정이 아니라 **로컬 `git config`** 였다. 웹 UI 로 만든
커밋은 GitHub 이 noreply 를 붙여 주지만, 로컬에서 만든 커밋 20건은 전역 config 의 실제
주소를 그대로 실어 보냈다. 계정 설정을 믿고 있었지만 커밋을 만드는 경로가 둘이었다.

**근거.** 공개 레포의 커밋 메타데이터는 누구나 조회할 수 있고 `.patch` 엔드포인트로
스크래핑된다. 앞으로의 커밋만 막는 것으로는 이미 나간 20건이 남는다. 되돌리려면
**히스토리 재작성밖에 없고**, 그건 커밋 해시를 전부 바꾸는 비가역 작업이다. 이 레포는
협업자가 없는 22커밋짜리 개인 프로젝트라 force push 비용이 거의 없다고 판단했다.

**결과.** 백업 번들을 먼저 만든 뒤 `git filter-repo` 로 author/committer 신원을
치환하고, 커밋 메시지의 세션 링크와 과거 블롭에 남아 있던 실제 Vault 디렉터리 이름까지
같은 작업에서 제거했다. 검증은 로컬 확인으로 끝내지 않고 **GitHub 에서 새로 클론한
사본과 REST API 응답**으로 이중 확인했다 — 로컬 객체를 재사용하면 원격 상태를 검증한
것이 아니기 때문이다. 재작성 전후로 커밋 수(당시 22개)와 테스트가 모두 그대로였다.
다만 **완전 제거는
보장되지 않는다**는 한계도 함께 기록했다: Events API 캐시와 dangling 커밋은 남을 수
있고, 그것까지 지우려면 GitHub Support 의 GC 가 필요하다. 재발 방지로 커밋 전 체크리스트에
author 이메일 확인을 추가했다.

> `CLAUDE.md` "커밋 전 확인"

---

## 이 프로젝트에서 반복적으로 적용한 원칙

위 8건을 관통하는 것들이다.

| 원칙 | 나타난 곳 |
|---|---|
| **오류의 방향을 먼저 본다** — 되돌릴 수 있는 실수와 없는 실수를 다르게 다룬다 | 게이트는 재현율 우선(3), 정규화는 정밀도 우선(4) |
| **증상을 고치기 전에 원인을 잰다** | temperature 미설정 발견(1), 누락 목록 대조(2) |
| **조용히 틀리는 것을 코드로 막는다** | 골든셋 draft/confirmed(6), 어휘 동기화 테스트(5), 프롬프트 규칙 테스트(7) |
| **재지 못한 것을 잰 것처럼 쓰지 않는다** | `null` vs 0(6), 1건으로 편향 판단 안 함(2), 반복 채점을 표본 증가로 세지 않음(2) |
| **판정 기준을 데이터보다 먼저 고정한다** | v3 검증의 사전 등록 가설·임계값, v2 대조군(2) |
| **기록을 사후에 고치지 않는다** | 규칙 이전 라벨 보존(6), 결정 로그는 번복 시 상태만 변경 |

---

## 아직 풀지 못한 것

면접에서 물어보면 답할 수 있도록 정리해 둔다. 전체 목록은
[README "다음 결정이 필요한 것"](README.md#다음-결정이-필요한-것) 에 있다.

- **골든셋이 1건이다.** 지금 `field_accuracy` 는 한 필드가 전체를 좌우한다. 목표는 30건이고,
  경계 사례를 우선 넣는다는 규칙만 정해 둔 상태다.
- **judge 비결정성은 부분만 쟀다.** Opus 5 가 `temperature` 를 거부해 게이트처럼 고정할
  수단이 없다. 15회 실측에서 완결성·충실성은 **변동 0회**, 간결성만 8회 중 2회 갈렸다 —
  **가정보다 훨씬 작고 축마다 달랐다.** 그래서 다수결(비용 N배)이나 모델 교체(품질 하락)는
  하지 않기로 했지만, **항목 1건 기준이라 "결정적이다"로 뒤집지도 않았다.**
- **자기 편향은 아직 못 쟀다.** judge 쪽 편차는 15회로 쟀지만 **사람 점수는 여전히 n=1** 이고,
  그 1점에는 루브릭 밖 기준이 섞여 있다. 남은 완결성 격차 +1.0 이 편향인지 그 오염분인지
  구분되지 않는다. 사람 쪽 재라벨링 없이는 답이 안 나오는 문제다.
- **프롬프트 캐싱 미적용.** 세 호출 지점 모두 `cache_read=0` 이다. system 프롬프트가 매 호출
  동일하므로 배치 실행 시 가장 큰 절감 지점이지만, 실행 건수가 적어 아직 이득이 없다.
- **`title_only` 피드.** 본문을 주지 않는 소스(중앙값 0자·97자)가 있어 추출 품질이 소스에
  좌우된다. 본문 fetch 단계를 넣으면 의존성과 실패 모드가 늘어난다.
