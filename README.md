# ai-news-ontology

AI 뉴스·논문·업계 소식을 자동 수집해 **온톨로지 스키마로 구조화**하고,
**LLM-judge 기반 eval 로 요약 품질을 검증**한 뒤, **Obsidian Vault 에 마크다운
노트로 적재**하는 개인 파이프라인.

> **현재 상태** — 수집 → 관련성 게이트 → 추출 → 기업명 정규화 → Obsidian 적재까지
> 한 건이 실제로 끝까지 흐른다. **품질 검증(eval) 실행 파이프라인은 아직 없다.**
>
> | 단계 | 상태 |
> |---|---|
> | `collectors/` — RSS·Atom 수집, 소스별 본문 품질 표기 | 동작 |
> | 관련성 게이트 — Haiku 4.5, `temperature: 0` 고정 | 동작 |
> | 추출 — Opus 5, `extract_ontology.v3.md` | 동작 |
> | `extraction/normalize.py` — 기업명 alias 정규화 | 동작 |
> | `obsidian_writer/` — frontmatter + 위키링크 노트 적재 | 동작 |
> | `observability/` — 스킵·미등록 기업 JSONL 기록 | 동작 |
> | `eval/` — 골든셋 대조 + LLM-judge 실행 | **미구현** (형식과 루브릭만 있다) |
> | Langfuse trace, 토큰·비용 집계 | 미구현 |
>
> 골든셋은 아직 **후보 2건**뿐이고 사람이 라벨링한 정답은 0건이다
> ([`eval/golden_set/README.md`](eval/golden_set/README.md)).

## 왜 만드는가

AI 소식은 양이 많고 휘발성이 높다. 읽고 나면 남지 않는다. 이 파이프라인의 목표는
"많이 읽는 것"이 아니라 **나중에 다시 찾을 수 있는 형태로 남기는 것**이다.
그래서 두 가지를 고집한다.

1. **구조화** — 자유 텍스트 요약만 쌓으면 검색이 안 된다. 고정된 축(기술영역,
   발표유형 등)으로 태깅해야 "작년에 봤던 RAG 관련 오픈소스 발표"를 찾을 수 있다.
2. **검증** — LLM 요약은 그럴듯하게 틀린다. 골든셋과 LLM-judge 로 품질을 측정하지
   않으면, 조용히 나빠지는 것을 알아챌 방법이 없다.

## 아키텍처

```
┌──────────────┐
│  외부 소스    │  RSS 피드 / arXiv API
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│ collectors/                                  │  판단 없음. 원문만 가져온다.
│  rss.py · arxiv.py · dedup.py                │
└──────┬───────────────────────────────────────┘
       │ RawItem(url, title, body, source, published_at)
       ▼
┌──────────────────────────────────────────────┐
│ extraction/                                  │  LLM 호출 지점 ①
│  extractor.py ──▶ LLMClient (llm.py)         │
│  normalize.py  기업명 alias 정규화            │
│  schema.py     ★ 온톨로지 SSoT               │
└──────┬───────────────────────────────────────┘
       │ NewsNote  (pydantic 검증 통과분만)
       │
       ├──────────────────────────┐
       ▼                          ▼
┌──────────────────────┐   ┌──────────────────────────┐
│ eval/                │   │ obsidian_writer/         │
│  golden_set/  정답    │   │  template.py  frontmatter │
│  judge_prompts/ 루브릭 │   │  linking.py   [[위키링크]] │
│  runner.py    채점    │   │  writer.py    원자적 쓰기  │
│  → scores/ (gitignore)│   └──────────┬───────────────┘
└──────────────────────┘              │
        │  LLM 호출 지점 ②             ▼
        │                      ┌──────────────────┐
        │                      │ Obsidian Vault   │
        │                      │ (.env 로 경로 주입)│
        │                      └──────────────────┘
        ▼
┌──────────────────────────────────────────────┐
│ observability/   trace · 토큰/비용 · 실패 로그  │  전 단계를 가로지름
└──────────────────────────────────────────────┘
```

검증 실패(ValidationError, 임계값 미달)는 **노트를 쓰지 않고 격리**한다.
조용히 통과시키면 Vault 가 오염되고, 그때부터 이 시스템은 신뢰할 수 없어진다.

## 폴더별 역할

| 경로 | 역할 |
|---|---|
| `collectors/` | RSS·arXiv 수집, 중복 제거. **요약·분류하지 않는다** |
| `extraction/` | 요약 + 온톨로지 5필드 구조화. `schema.py` 가 스키마 SSoT |
| `eval/` | 품질 검증. 골든셋 대조(결정적) + LLM-judge(요약) |
| `eval/golden_set/` | 사람이 라벨링한 정답. **커밋 대상** |
| `eval/judge_prompts/` | judge 루브릭. 버전 고정, 수정 대신 v2 생성 |
| `eval/scores/` | 실행 결과. **gitignore** |
| `obsidian_writer/` | Vault 마크다운 적재. frontmatter + 위키링크 |
| `observability/` | trace, 토큰/비용, 실패 로그. Langfuse 없으면 no-op |
| `tests/` | 계약 테스트 (어휘 동기화, 스키마 round-trip) |

루트 파일: `config.yaml`(비밀 아닌 설정) · `.env.example`(비밀) ·
`schema.md`(스키마 문서) · `pyproject.toml`

## 시작하기

```bash
python -m venv .venv && .venv\Scripts\activate   # Windows
pip install -e ".[dev]"

cp .env.example .env      # ANTHROPIC_API_KEY, OBSIDIAN_VAULT_PATH 채우기
```

설정은 두 곳으로 나뉜다.
- **`.env`** — 비밀과 머신별 경로. 커밋하지 않는다.
- **`config.yaml`** — 소스 목록, 통제어휘, 임계값. 커밋하고 리뷰한다.

## 온톨로지 스키마

5개 필드: `기술영역`(통제어휘 14) · `발표유형`(통제어휘 6) · `관련기업`(자유 +
alias 정규화) · `관련기존기술`(자유 태그) · `영향도`(1~5 + 근거).

전체 정의와 판단 기준은 **[`schema.md`](schema.md)**,
코드 정의는 **[`extraction/schema.py`](extraction/schema.py)**.
두 문서는 1:1 대응하며 `tests/test_vocab_sync.py` 가 일치를 검증한다.

---

## 결정 로그 (Decision Log)

설계 판단과 그 근거. 나중에 "왜 이렇게 했지"를 되짚기 위한 기록이며,
번복할 때는 지우지 말고 **상태를 `번복됨`으로 바꾸고 사유를 남긴다.**

| ID | 날짜 | 결정 | 근거 | 대안 | 상태 |
|---|---|---|---|---|---|
| D-001 | 2026-08-29 | 통제어휘와 자유 필드를 섞는다 | 전부 닫으면 현실과 어긋나고, 전부 열면 태그가 파편화돼 검색이 안 된다 | 전부 자유 태그 / 전부 고정 | 유효 |
| D-002 | 2026-08-29 | 어휘 SSoT 는 `schema.py` Enum. `config.yaml`·`schema.md` 는 미러이며 테스트로 동기화 검증 | 세 곳에 같은 어휘가 있는 건 드리프트 위험. 그렇다고 yaml 을 SSoT 로 하면 타입 안전성을 잃는다 | yaml 을 SSoT 로 두고 런타임 로딩 | 유효 |
| D-003 | 2026-08-29 | 코드 필드명은 영어, 한국어는 pydantic alias | 코드는 영어가 다루기 쉽고, Obsidian frontmatter 는 한국어가 검색에 낫다 | 코드도 한국어 필드명 | 유효 |
| D-004 | 2026-08-29 | LLM 호출을 `LLMClient` Protocol 뒤로 감춘다 | 기본은 Anthropic 이지만 judge 를 다른 모델로 바꿔 편향을 확인할 여지를 남긴다 | SDK 직접 호출 | 유효 |
| D-005 | 2026-08-29 | 평가 패키지명을 `eval` 로 둔다 | 내장 `eval()` 은 모듈 네임스페이스를 가리지 않아 실제 충돌이 없다. 폴더명의 명확성이 낫다 | `evaluation/` 으로 개명 | 유효 |
| D-006 | 2026-08-29 | 채점을 골든셋 대조와 LLM-judge 두 축으로 분리 | 요약은 정답이 하나가 아니라 exact match 가 무의미하고, 통제어휘 필드는 judge 를 쓸 이유가 없다 | 전부 judge 로 채점 | 유효 |
| D-007 | 2026-08-29 | frontmatter 에서 `영향도`를 점수/근거 두 키로 편다 | Obsidian Properties·Dataview 가 중첩 객체를 정렬·필터로 잘 못 다룬다 | 중첩 객체 그대로 직렬화 | 유효 |
| D-008 | 2026-08-29 | observability 는 선택적 의존성. 키 없으면 no-op | 관측 도구의 부재나 장애가 파이프라인 장애가 되면 안 된다 | Langfuse 필수 의존성 | 유효 |
| D-009 | 2026-08-29 | 구조화 출력은 **structured outputs**(`messages.parse(output_format=NewsOntology)`)로. strict tool use 를 쓰지 않는다 | 이건 도구 호출이 아니라 단일 추출이다. tool use 는 "모델이 도구를 부르도록 유도"하는 우회로라 도구를 안 부르고 텍스트로 답하는 분기를 늘 방어해야 한다. `parse` 는 pydantic 모델을 그대로 넘기고 검증된 객체를 받으므로 schema.py 와 런타임 사이에 손으로 옮겨 적는 단계가 없다(D-002와 일관) | strict tool use / 프롬프트로 JSON 유도 후 자체 파싱 | 유효 |
| D-010 | 2026-08-29 | 추출 프롬프트를 `extraction/prompts/*.vN.md` 로 분리하고 judge 와 같은 버저닝 관례 적용 | 프롬프트가 바뀌면 eval 점수도 바뀐다. 어떤 버전에서 나온 점수인지 추적 못 하면 eval 자체가 의미를 잃는다 | 코드에 문자열로 하드코딩 | 유효 |
| D-011 | 2026-08-29 | `CompanyRef.정규명`을 optional 로 바꾸고 없으면 `원문표기`를 승계 | 원래 필수라서 LLM 이 정규화까지 하게 돼 있었다. 결정적으로 처리 가능한 일을 확률적 모델에 맡기는 셈. 정규화는 `normalize.py` 의 책임으로 남긴다 | LLM 이 정규명까지 생성 | 유효 |
| D-012 | 2026-08-29 | 프롬프트의 통제어휘 목록을 하드코딩하지 않고 schema.py Enum 에서 런타임 주입 | 하드코딩하면 어휘 미러가 네 곳(코드/config/문서/프롬프트)이 된다. 프롬프트는 테스트하기도 어려운 미러라 가장 먼저 썩는다 | 프롬프트에 목록 직접 기재 | 유효 |
| D-013 | 2026-08-29 | RSS 소스에 `body_quality` 를 표기하고, 슬라이스 검증 기준 소스를 arXiv Atom API 로 둔다 | 실측 결과 벤더 블로그 피드 다수가 **본문을 주지 않는다**(50건 중앙값: HF 0자, DeepMind 97자, OpenAI 149자). 본문 fetch 단계 없이는 추출 품질이 소스에 좌우된다 | 모든 피드를 동일 취급 | 유효 |
| D-014 | 2026-08-29 | **GeekNews**(`news.hada.io/rss/news`)를 소스에 추가 | `title_only` 피드가 많아 본문 커버리지가 부족했다. 실측 50건 **중앙값 160자·0자 항목 없음**으로 DeepMind(97자, 15건이 0자)·HF(전부 0자)보다 안정적이고, 한국어 소스라 기존 영어 소스와 표기 다양성도 확보된다. 다만 40/50건이 말줄임으로 잘려 있어 `truncated` 로 표기 | 벤더 블로그만 유지 / 본문 fetch 먼저 구현 | 유효 |

| D-015 | 2026-08-29 | 정밀 추출 **앞단에 저비용 관련성 게이트**를 둔다. `llm:` 블록을 `relevance_gate`(Haiku 4.5) / `extraction`(Opus 5)로 분리 | GeekNews 는 최근 50건 중 약 60%만 AI 관련이다(D-014). 나머지를 Opus 5·effort high 로 넘기면 버려질 결과에 비용을 쓴다. 게이트는 "분석할 가치가 있는가" 이분 판정이라 저비용 모델로 충분하다. 게이트에 분류까지 시키지 않은 이유는 저비용 모델의 분류가 정밀 추출 결과와 섞이면 어느 쪽 품질을 재는지 알 수 없어지기 때문 | 수집 단계에서 키워드로 필터링 / 전량 정밀 추출 / 게이트가 분류까지 수행 | 유효 |
| D-016 | 2026-08-29 | 게이트가 버린 항목도 `SkipRecord` 로 남긴다 (인터페이스만, 기본은 no-op) | 스킵이 흔적 없이 사라지면 게이트의 **오탐을 검증할 방법이 없다**. "애매하면 통과"(재현율 우선) 결정이 실제로 지켜지는지 표본 검토하려면 무엇을·왜·어떤 모델이·언제 버렸는지가 필요하다 | 스킵을 그냥 버림 / 즉시 파일 로깅 구현 | 유효 |
| D-017 | 2026-08-29 | GeekNews 를 **필터링 없이 전량 수집**한다 (걸러내는 일은 게이트가 한다) | 제목에 AI 관련성이 드러나지 않는 글이 많아 수집 단계 키워드 필터는 놓치는 게 많다. 개발자 커뮤니티에서 실제로 많이 참고하는 소스라 소스 자체를 빼는 것도 손해다. 판정은 본문을 읽을 수 있는 게이트로 미룬다 | 수집 단계 키워드 필터 / 소스 제외 | 유효 |
| D-018 | 2026-08-29 | `effort`/`thinking` 을 모델별로 끌 수 있게 optional 로 변경 | Haiku 4.5 는 `output_config.effort` 를 거부한다(400). 단계별 모델을 두는 순간 "모든 모델이 같은 파라미터를 받는다"는 가정이 깨진다 | 모든 단계에 동일 파라미터 전송 | 유효 |

| D-019 | 2026-08-29 | `Usage` 에 `thinking_tokens` / `latency_s` / 캐시 토큰을 분리 기록 | 합계만 보면 "왜 비싼지"를 모른다. 첫 실측에서 Opus 5 출력 483토큰 중 251이 thinking 이었고 입력 6,160토큰의 대부분이 고정 프롬프트였다 — 어느 쪽을 손봐야 하는지가 분리해 봐야 보인다. thinking 은 `output_tokens` 의 **부분집합**이라 비용 계산 시 더하지 않는다 | 합계만 기록 | 유효 |

| D-020 | 2026-08-29 | `관련기존기술` 표기를 **영어 표준형으로 통일**한다. 사전이 아니라 **프롬프트 규칙**으로 강제 | 첫 추출에서 한 기사 안에서만 표기가 세 갈래로 갈렸다(`퍼징(Fuzzing)` 병기 / `바이브코딩` 순한글). 기업명(D-011)과 달리 이 필드는 **어휘 규모를 아직 모른다** — 규모를 모르는 채 사전을 먼저 만들면 틀린 사전이 되고 유지 비용이 개인 프로젝트에서 감당이 안 된다. 생성 시점에 표기를 맞추는 쪽이 싸다. 병기를 금지한 것은 그게 세 번째 변종을 만들기 때문 | alias 사전 도입 / 한국어 우선 / 표기 자유 방치 | 유효 |
| D-021 | 2026-08-29 | `발표유형` 에 **`Community/Discussion`** 추가 (6개 → 7개) | 첫 추출에서 커뮤니티 발 버그 발견 글이 갈 곳이 없어 `Benchmark/Report` 로 밀려났다. GeekNews 를 유지하기로 한 이상(D-017) 이 부류는 계속 들어온다. 가르는 기준은 "무엇을"이 아니라 **"누가"** — 공식 조직이면 다른 값, 개인·커뮤니티면 이 값. `Incident/BugReport` 도 후보였으나 관측된 빈 칸은 "버그"보다 "커뮤니티 발 글"이 더 정확한 설명이었다 | `Incident/BugReport` 추가 / 어휘 유지하고 오분류 감수 | 유효 |
| D-022 | 2026-08-29 | `기술영역: Agent` 판단 기준을 **문서·프롬프트 층위에만** 둔다. 스키마(Enum)는 그대로 | 첫 추출에서 LLM 보조로 작성한 퍼저가 `Agent` 로 분류됐다. 기준을 좁혀야 하지만 **어휘를 쪼개면 과거 노트의 라벨이 소급해서 틀린 것이 된다.** 판단 기준은 사례가 쌓이며 계속 바뀌는 종류라 문서가 맞는 층위다 | `Agent` 를 세분화 / 기준 없이 방치 | 유효 |
| D-023 | 2026-08-29 | 프롬프트를 `extract_ontology.v2.md` 로 올리고 v1 은 보존 | D-020·D-021·D-022 가 모두 프롬프트 텍스트를 바꾼다. v1 은 이미 실제 추출 1건에 쓰였으므로, 그 출력이 어떤 지시에서 나왔는지 추적 가능해야 한다 (D-010) | v1 직접 수정 | 유효 |

| D-024 | 2026-08-29 | v2 프롬프트 실전 검증 완료 — D-020·D-021·D-022 **세 규칙 모두 라벨을 바꿨다** | 같은 기사(#33001) 재추출에서 `기술영역` 은 `Agent` 가 빠지고, `발표유형` 은 `Benchmark/Report`→`Community/Discussion`, `관련기존기술` 은 `퍼징(Fuzzing)`/`바이브코딩`→`Fuzzing`/`Vibe Coding` 로 바뀌었다. 프롬프트 규칙만으로 표기 통일이 되는지 의심스러웠으나(사전 없이) 1건에서는 정확히 작동 | — | 유효 |

| D-025 | 2026-08-29 | `normalize.py` 구현. **퍼지 매칭을 쓰지 않고** 사전에 명시적으로 등록된 표기만 정규화한다. 흡수하는 차이는 대소문자·공백뿐 | 오류가 비대칭이다. 정규화를 **놓치면** `정규화성공=false` 로 남고 원문이 보존돼 나중에 사전을 보강하면 복구된다(손실은 그래프 노드 하나 더). 정규화를 **잘못하면** 서로 다른 기업이 한 노드로 합쳐지고, 합쳐진 뒤에는 원래 무엇이었는지 알 수 없다. 미탐은 되돌릴 수 있고 오탐은 되돌릴 수 없으므로 정밀도를 재현율보다 우선한다 — 관련성 게이트(D-015)가 재현율을 우선하는 것과 정반대의 이유다 | 편집거리·부분문자열 기반 퍼지 매칭 / LLM 에게 정규화를 맡김 | 유효 |
| D-026 | 2026-08-29 | `CompanyRef` validator 가 `canonical`/`resolved` 를 **입력값과 무관하게 항상 재계산**한다 | 사전이 이 두 필드의 단일 출처여야 같은 원문 표기가 언제나 같은 대표명으로 떨어진다. 입력값을 존중하면 LLM 이 보낸 값과 사전 결과가 섞여 어느 쪽이 적용됐는지 알 수 없어진다 | 값이 없을 때만 채우기 | 유효 |

| D-027 | 2026-08-29 | `NewsOntology` 에 **`요약` 필드 추가**(옵션 A). `영향도 근거`를 재사용하지 않는다 | 두 필드는 **역할이 다르다.** 요약은 "무슨 일이 있었나"(사실), 영향도 근거는 "왜 중요한가"(판단)다. 근거를 요약 자리에 쓰면 노트를 다시 열었을 때 무슨 내용이었는지 알 수 없고, 반대로 요약을 근거에 쓰면 eval 이 영향도 점수를 채점할 수 없다. 스타일은 평서문 `~다`, 2~3문장, 메타 표현(`이 기사는`) 금지로 고정 | 옵션 B: `impact_rationale` 재사용 | 유효 |
| D-028 | 2026-08-29 | 같은 기사 노트가 이미 있으면 **`skip`**(기본). 덮어쓰기는 `config.yaml` 에서 명시적으로만. 동일성 판정은 `source_url` | Vault 는 사용자의 실제 데이터이고 Obsidian 에서 손으로 고쳤을 수 있다. 재실행이 사용자의 편집을 지우면 안 된다. 제목이 비슷해 **슬러그만 겹치는 경우는 별개 노트**이므로 `-2`, `-3` 을 붙인다 — 두 상황을 같은 정책으로 다루면 서로 다른 기사가 서로를 덮어쓴다 | 항상 덮어쓰기 / 슬러그 충돌도 skip | 유효 |
| D-029 | 2026-08-29 | frontmatter 키는 **영문**으로 쓴다 (`impact_score`, `tech_domain` …) | D-003 은 "Obsidian 검색에 한글 키가 낫다"고 봤으나, 실제로 Dataview 쿼리(`WHERE impact_score > 3`)와 템플릿에서 한글 키는 매번 따옴표로 감싸야 한다. 노트에서 사람이 읽는 것은 **값**이지 키가 아니다 | 한글 키 유지 (D-003) | 유효 — **D-003 의 frontmatter 부분을 대체** |
| D-003 | 2026-08-29 | ~~코드 필드명은 영어, 한국어는 pydantic alias~~ | 코드 필드명 규칙은 그대로 유효하다. 다만 "**한국어는 frontmatter 로 나간다**"는 부분은 D-029 로 번복됐다 — frontmatter 키는 영문이고, 한국어 alias 는 LLM 이 보는 스키마와 로그에서만 쓴다 | | **부분 번복** (D-029) |
| D-030 | 2026-08-29 | `관련기업`도 위키링크로 만든다. 단 링크 대상은 **canonical** | 기업을 링크하면 "이 기업이 언급된 노트" 그래프가 공짜로 생긴다. 원문표기로 링크하면 `오픈AI` 와 `OpenAI` 가 다른 노드가 되어 D-025 로 정규화한 것을 그래프에서 도로 갈라 놓는다. frontmatter 의 `companies` 도 같은 이유로 canonical 을 싣는다 | 기업은 메타데이터로만 / 원문표기로 링크 | 유효 |
| D-031 | 2026-08-29 | 파일명 슬러그에서 **한글을 로마자로 옮기지 않는다** | 로마자 표기법이 여러 개라 같은 제목이 실행마다 다른 파일명이 될 수 있고, 변환된 파일명은 사람이 Vault 탐색기에서 못 읽는다. 길이만 제한하고(하이픈 경계에서 자름) 한글은 그대로 둔다 | romanize 라이브러리 도입 | 유효 |

| D-032 | 2026-08-29 | 관련성 게이트에 **`temperature: 0`** 고정. 추출 단계는 건드리지 않는다 | 게이트 판정이 흔들린 원인은 프롬프트가 아니라 **샘플링이었다.** 설정에 temperature 항목이 없었고 API 기본값(1.0)이 적용되고 있었다. 게이트는 "분석할 가치가 있는가"라는 이분 판정이라 창의성이 필요 없고, 같은 기사가 실행마다 다르게 처리되면 파이프라인을 신뢰할 수 없다. 적용 후 5회 재실행에서 판정과 근거 텍스트가 **바이트 단위로 동일**했다 | 프롬프트에 "애매하면 통과" 문구 보강 / 다수결로 3회 호출 | 유효 |
| D-033 | 2026-08-29 | `temperature` 는 `extra_body` 로 실어 보낸다 | SDK 1.x 의 `messages.create`/`parse` 에는 `temperature` **명명 인자가 아예 없다** — 최신 모델에서 제거된 파라미터이기 때문. 구세대 모델(Haiku 4.5)은 여전히 받으므로 `extra_body` 가 유일한 경로다. **Opus 5 는 400 으로 거부한다**(`temperature is deprecated for this model`) — 단계별로 모델이 다르면 파라미터 지원 범위도 다르다는 D-018 의 두 번째 사례 | SDK 버전 다운그레이드 / 게이트도 Opus 로 통일 | 유효 |

### 다음 결정이 필요한 것
- [x] ~~**게이트 판정이 경계 사례에서 흔들린다**~~ → D-032. 원인은 temperature 미설정(기본 1.0). 고정 후 5/5 동일
- [x] ~~**`normalize.py` 미구현**~~ → D-025. `OpenAI` 가 `정규화성공=true` 로 확인됨
- [x] ~~**프롬프트 v2 로 재추출해 v1 출력과 비교**~~ → D-024. 세 규칙 모두 라벨을 바꿈
- [ ] **`unknown_company` 관측 연결** — 정규화 실패(`resolved=false`)를 `observability` 로 흘려 사전 보강 큐를 만든다. 첫 2건에서 `FFmpeg`·`Cursor`·`SpaceX` 가 미등록으로 나왔다
- [ ] **`발표유형` 에 "계약·공급 중단" 부류가 필요한가** — #33003(OpenAI→Cursor 모델 공급 종료)이 `Funding/M&A` 로 밀렸다. 1차 사건은 인수가 아니라 계약 종료다. D-021 과 같은 형태의 빈 칸일 수 있으나 1건이라 판단 보류
- [ ] **`역할` 예시에 `인수 주체` 추가** — 같은 기사에서 SpaceX(인수 주체)가 `경쟁사` 로 분류됐다. 프롬프트 예시 5개에 해당 값이 없다
- [ ] **프롬프트 캐싱을 켤 것인가** (D-019) — 첫 실측에서 `cache_read=0`, Opus 입력 6,160토큰 중 대부분이 매 호출 동일한 system 프롬프트다. 배치로 돌리면 여기가 가장 큰 절감 지점
- [x] ~~**`발표유형` 어휘에 빈 칸이 있는가**~~ → D-021 로 `Community/Discussion` 추가
- [ ] **게이트 판정의 정확도를 어떻게 잴 것인가** (D-015) — 게이트도 LLM 판정이므로 골든셋이 필요하다. 온톨로지 골든셋과 별도로 관련성 라벨을 붙일지, 같은 파일에 필드를 추가할지
- [ ] **본문 fetch 단계를 넣을 것인가** (D-013) — `title_only` 피드를 살리려면 원문 페이지를 가져와야 한다. collectors 의 "판단하지 않는다" 원칙은 유지되지만 의존성(httpx/trafilatura)과 실패 모드가 늘어난다
- [x] ~~**비-AI 항목 필터링** (D-014)~~ → D-015/D-017 로 해결. 수집은 전량, 판정은 저비용 게이트
- [ ] **자유 태그(`관련기존기술`)의 언어 정책** — 한국어 소스가 들어오면서 `Transformer` 와 `트랜스포머` 가 갈릴 수 있다. 프롬프트에서 영어 표기로 고정할지, 태그에도 alias 사전을 둘지 (기업명은 D-011 로 정리됨)
- [ ] 재실행 시 중복 판정 기준 — URL 해시만으로 충분한가, 제목 유사도까지 볼 것인가
- [ ] eval 실행 주기 — 매 실행마다인가, 프롬프트/모델 변경 시에만인가 (비용 문제)
- [ ] judge 모델을 extractor 와 다른 모델로 둘 것인가 (자기 편향 회피 vs 비용)
- [ ] 골든셋 라벨링을 어디까지 자동 초안화할 것인가 (LLM 초안 → 사람 수정)
