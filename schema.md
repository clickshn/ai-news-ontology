# 온톨로지 스키마 (v0.1 초안)

AI 뉴스/논문 1건을 구조화하는 5개 필드의 정의.

> **이 문서와 `extraction/schema.py` 는 1:1 대응한다.** 한쪽만 고치지 말 것.
> 통제어휘의 단일 출처(SSoT)는 `extraction/schema.py` 의 `StrEnum` 이고,
> `config.yaml: ontology` 와 이 문서는 사람이 읽기 위한 미러다.
> 일치 여부는 `tests/test_vocab_sync.py` 가 검증한다.

## 설계 원칙

1. **폐쇄형과 개방형을 섞는다.** 집계·필터에 쓸 필드(기술영역, 발표유형)는 고정
   어휘로 닫고, 세계가 계속 늘어나는 필드(기업, 기존기술)는 열어둔다. 전부 닫으면
   금방 현실과 어긋나고, 전부 열면 태그가 파편화돼 검색이 안 된다.
2. **자유 필드도 그냥 두지 않는다.** 기업명은 alias 매핑으로 정규화하고, 원문
   표기를 함께 보존해 정규화가 틀렸을 때 되짚을 수 있게 한다.
3. **주관적 판단에는 근거를 붙인다.** 영향도는 점수만으로 검증이 불가능하므로
   한 줄 근거를 필수로 받는다. 근거가 없는 점수는 eval 이 채점할 수 없다.

---

## 수집 대상 범위 (관련성 기준)

아래 5필드로 구조화할 **가치가 있는 글**의 기준. `RelevanceGate` 와
`prompts/relevance_gate.v1.md` 가 이 절을 구현한다.

> **한 문장 기준: AI Engineer 직무에 직접 쓸모가 있거나, 업무자동화에 쓸모가 있는가.**

### 포함 (is_relevant = true)
| 범주 | 예시 |
|---|---|
| LLM·모델 | 신규 모델, 아키텍처, 학습/추론 기법, 벤치마크 |
| AI 엔지니어링 | RAG, 에이전트, 프롬프트, 평가, 서빙, 파인튜닝, MLOps |
| AI 코딩 도구 | Claude Code, Cursor, Copilot, Codex 등 개발 도구와 그 사용기 |
| 업무자동화 | n8n·Zapier 류 워크플로, 자동화 스크립트, 데이터 파이프라인 |
| AI 산업 | 모델 제공사의 정책·계약·투자·규제 (엔지니어의 선택에 영향) |
| AI 인프라 | GPU/칩, 데이터센터, 서빙 비용 구조 |

### 제외 (is_relevant = false)
| 범주 | 예시 |
|---|---|
| AI 무관 개발 | 언어·프레임워크 릴리스, DB, 네트워크, 브라우저, OS |
| 일반 기술 뉴스 | 하드웨어 리뷰, 게임, 보안 사고(AI 무관), 커뮤니티 잡담 |
| 회고·에세이 | 특정 기술 주장 없이 감상만 있는 글 |

### 경계 판단
- **AI가 도구로만 쓰인 글**(예: "AI로 만든 퍼저가 FFmpeg 버그를 찾았다") → **포함**.
  AI 적용 사례는 AI Engineer 직무에 직접 쓸모가 있다.
- **AI가 소재로만 언급된 글**(예: "AI 때문에 전력 요금이 올랐다") → 제외.
- **애매하면 포함한다.** 게이트의 목적은 정밀 추출 비용 절감이지 큐레이션이
  아니다. 잘못 버린 글은 되돌릴 수 없지만, 잘못 통과시킨 글은 추출 단계에서
  걸러지고 비용만 조금 더 든다. **비대칭적 비용이므로 재현율을 정밀도보다 우선한다.**

---

## 필드 요약

| 필드 | 한국어 키 | 코드 필드 | 타입 | 어휘 | 개수 |
|---|---|---|---|---|---|
| 기술영역 | `기술영역` | `tech_domains` | `list[TechDomain]` | 통제어휘 (14) | 1~3 |
| 발표유형 | `발표유형` | `release_type` | `ReleaseType` | 통제어휘 (8) | 정확히 1 |
| 관련기업 | `관련기업` | `companies` | `list[CompanyRef]` | 자유 + alias 정규화 | 0~n |
| 관련기존기술 | `관련기존기술` | `prior_art` | `list[str]` | 자유 태그 (영어 표준형) | 0~8 |
| 영향도 | `영향도` | `impact` | `Impact` | 1~5 리커트 + 근거 | 1 |

---

## 1. 기술영역 (`tech_domains`) — 통제어휘

무엇에 **관한** 소식인가. 가장 관련 깊은 것부터 **1~3개**. 3개를 넘으면 그건 대개
개별 소식이 아니라 주간 요약이므로, 상한을 3으로 강제해 라벨 인플레이션을 막는다.

| 값 | 포함 범위 | 경계 사례 판단 |
|---|---|---|
| `LLM` | 신규 모델 공개, 아키텍처, 사전학습, 스케일링 | 모델 "출시"는 `LLM` + 발표유형 `ProductLaunch` |
| `Multimodal` | 비전/오디오/비디오 결합, 이미지·영상 생성 | 텍스트 전용이면 `LLM` |
| `RAG` | 검색 증강, 벡터DB, 임베딩, 문서 QA | 검색이 도구 호출로만 쓰이면 `Agent` |
| `Agent` | 툴 사용, 자율 루프, 멀티에이전트, 컴퓨터 사용 | 단발 함수 호출 기능은 `LLM`. 아래 "Agent 로 분류하는 기준" 참고 |
| `Reasoning` | 사고 시간 확장, CoT, 추론 특화 학습 | 벤치마크 점수만 다루면 `Eval/Governance` |
| `Training/Finetuning` | 사후학습, RLHF/RLAIF, DPO, PEFT, 증류 | |
| `Inference/Serving` | 서빙, 양자화, KV 캐시, 지연/처리량/비용 | 칩 자체는 `Hardware/Chip` |
| `Infra/MLOps` | 오케스트레이션, 파이프라인, 실험 관리 | |
| `Eval/Governance` | 벤치마크, 평가 방법론, 감사, 내부 거버넌스 | 정부 규제는 `Safety/Alignment` 아니고 발표유형 `Policy/Regulation` |
| `Safety/Alignment` | 정렬, 레드팀, 해석가능성, 오남용 대응 | |
| `Data/Synthetic` | 데이터셋 공개, 합성데이터, 데이터 라이선스 | 저작권 **소송**은 발표유형 `Policy/Regulation` |
| `Embodied/Robotics` | 로보틱스, 자율주행, 물리세계 상호작용 | |
| `Hardware/Chip` | GPU/TPU/NPU, 데이터센터, 전력 | |
| `Application/Product` | 최종 사용자 제품·서비스에의 적용 | 모델 자체 발표는 `LLM` |

### `Agent` 로 분류하는 기준

`Agent` 는 어휘 중 가장 넓게 오용되기 쉬운 값이다. "AI가 뭔가를 했다"는 소식은
많지만 그게 전부 에이전트는 아니다. 실제로 첫 추출 테스트에서 **LLM 보조로 작성한
퍼저**가 `Agent` 로 분류됐다 — 퍼저는 자율 루프도 도구 호출도 아니다.
(결정 로그 D-022)

**아래를 모두 만족할 때만 `Agent` 를 고른다.**
1. 시스템이 **여러 단계를 스스로 이어서** 실행한다 (사람이 매 단계를 지시하지 않는다)
2. **도구 호출 · 계획 수립 · 자기 수정** 중 하나 이상이 소식의 핵심이다

LLM 이 **보조 도구로 쓰였을 뿐**이면 `Agent` 가 아니다. 무엇에 적용됐는지를 보고
`Application/Product` 등 다른 값을 먼저 고려한다.

| 사례 | 분류 | 이유 |
|---|---|---|
| LLM 보조로 작성한 퍼저가 오픈소스 버그를 발견 | `Application/Product` | LLM 은 코드를 쓰는 데 쓰였을 뿐. 실행 주체가 자율적이지 않다 |
| AI 코딩 도구로 사이드 프로젝트 22개를 만들었다 | `Application/Product` | 사람이 매 단계를 지시했다 |
| 모델이 스스로 도구를 호출해 여러 단계로 이슈를 해결 | `Agent` | 자율 다단계 + 도구 호출 |
| 프레임워크가 멀티에이전트 오케스트레이션을 지원 | `Agent` | 에이전트 구조 자체가 주제 |

> 이 기준은 **문서와 프롬프트 층위에만** 둔다. 스키마(Enum)는 그대로다 —
> 판단 기준은 사례가 쌓이면서 계속 바뀌는데, 어휘를 쪼개면 과거 노트의 라벨이
> 소급해서 틀린 것이 된다. (결정 로그 D-022)

## 2. 발표유형 (`release_type`) — 통제어휘

소식의 **형태**. 정확히 1개. 여러 개로 보이면 "이 소식의 1차 사건"을 고른다.
(예: 논문과 함께 가중치를 공개했다 → 무엇이 헤드라인인지로 판단)

| 값 | 정의 | 경계 사례 판단 |
|---|---|---|
| `Paper` | 논문, 프리프린트, 기술 리포트 | |
| `ProductLaunch` | 상용 제품·기능·API 출시 및 업데이트 | |
| `OpenSource` | 가중치/코드/데이터의 공개 | |
| `Policy/Regulation` | 정책, 규제, 표준, 소송, 정부 발표 | |
| `Funding/M&A` | 투자 유치, 인수합병, 상장 | |
| `Benchmark/Report` | **공식 조직**이 낸 벤치마크 결과, 시장·산업 조사 보고서 | 개인·커뮤니티 측정치는 `Community/Discussion` |
| `Community/Discussion` | **커뮤니티·개인**의 버그 발견, 이슈 제기, 사용기, 토론, Show-and-tell | 공식 조직의 장애 공지는 `Policy/Regulation` 이나 `ProductLaunch` 쪽을 먼저 본다 |
| `Partnership/Contract` | **조직 간 거래 관계**의 체결·변경·종료. 모델·API 공급, 제휴, 라이선싱, 유통, 독점 계약 | 소유권이 옮겨가면 `Funding/M&A` — 아래 세 경계 참고 |

### `Partnership/Contract` 를 추가한 이유

`SpaceX의 Cursor 인수 이후 OpenAI가 내린 결정`(GeekNews #33003)이 `Funding/M&A`
로 밀려났다. 기사의 1차 사건은 **인수가 아니라 인수 이후의 모델 공급 계약 종료**
인데, 어휘 7개에 "조직 간 거래 관계가 바뀌었다"를 담을 값이 없었다.
(결정 로그 D-056)

**가르는 축은 "누가"가 아니라 "무슨 사건인가"다.** `Community/Discussion` 은
발신 주체로 갈랐지만(D-021), 이 값은 사건의 종류로 가른다.

> **소유권이 옮겨가면 `Funding/M&A`, 거래 관계가 바뀌면 `Partnership/Contract`.**

#33003 에 적용하면 — SpaceX 의 Cursor 인수는 소유권 이동(M&A)이지만 그건
**배경**이고, 헤드라인 사건은 OpenAI 의 공급 중단(거래 관계)이다. 기존
"1차 사건을 고른다" 규칙이 그대로 판정한다.

**종료만 담는 값(`Contract/Termination`)은 택하지 않았다.** 그러면 "두 회사가
공급 계약을 체결했다"가 갈 곳이 없어져 **두 번째 빈 칸을 보장**한다. 체결이
종료보다 흔하다는 점에서 더 나쁘다.

#### 경계 사례 3개

| 대치하는 값 | 가르는 기준 | 예 |
|---|---|---|
| `Funding/M&A` | **소유권**이 바뀌면 그쪽, **거래 관계**가 바뀌면 이쪽 | "A가 B를 인수" → `Funding/M&A` / "A가 B에 모델 공급을 중단" → `Partnership/Contract` |
| `ProductLaunch` | **사용자가 쓸 수 있는 것이 생겼으면** 그쪽, **조직 간 합의·종료 자체가 헤드라인이면** 이쪽 | "A 모델이 B 플랫폼에서 제공 시작(사용자 관점 기능 출시)" → `ProductLaunch` / "A와 B가 독점 공급 계약 체결" → `Partnership/Contract` |
| `Policy/Regulation` | 계약 분쟁이 **소송·규제**로 넘어갔고 그게 헤드라인이면 그쪽 | "계약 위반으로 제소" → `Policy/Regulation` |

### `Community/Discussion` 을 추가한 이유

첫 추출 테스트에서 `바이브코딩으로 만든 퍼저가 FFmpeg의 0 나누기 버그를 발견`
(GeekNews) 이 갈 곳이 없어 `Benchmark/Report` 로 밀려났다. 벤치마크도 산업
보고서도 아니었다. GeekNews 처럼 커뮤니티 발 글이 많은 소스를 유지하기로 한
이상(D-017) 이 부류는 계속 들어온다. (결정 로그 D-021)

**가르는 기준은 "무엇을"이 아니라 "누가"다.**
- 공식 조직(기업·기관·연구소)이 낸 것 → 내용에 맞는 다른 값
- 개인·커뮤니티가 올린 것 → `Community/Discussion`

버그·장애를 다루더라도 공식 발표가 아니면 `Community/Discussion` 이다.
`Incident/BugReport` 도 후보였지만, 관측된 빈 칸은 "버그"보다 "커뮤니티 발
글"이 더 정확한 설명이었다.

#### "누가"는 링크를 올린 사람이 아니라 **사건의 주체**다 (2026-09-01 명확화)

GeekNews 같은 애그리게이터는 커뮤니티가 링크를 올리지만, 그 링크가 가리키는
사건의 주체는 기업일 수 있다. 규칙을 문자 그대로 읽으면 **GeekNews 항목이 전부
`Community/Discussion` 이 되어 그 소스에서 어휘가 무의미해진다.**

- #33001(개인이 만든 퍼저가 버그를 발견) → 사건의 주체가 개인이므로
  `Community/Discussion`. 종전 판단 그대로 유효하다.
- #33003(OpenAI 가 공급 계약을 종료) → GeekNews 에 올라왔지만 사건의 주체는
  기업이므로 `Partnership/Contract`.

사례가 1건일 때는 드러나지 않던 모호함이고, 어휘를 늘리면서 부딪혔다.
스키마가 아니라 **문서·프롬프트 층위에서 좁히므로**(D-022) 과거 라벨은 그대로
유효하다. (결정 로그 D-057)

## 3. 관련기업 (`companies`) — 자유 엔티티 + alias 정규화

기업/기관은 계속 새로 생기므로 어휘를 닫을 수 없다. 대신 **표기 흔들림**을
정규화한다. `오픈AI` / `Open AI` / `OpenAI` 가 서로 다른 태그가 되면 Obsidian
그래프가 무의미해지기 때문이다.

### 정규화 방식

```
LLM 추출 원문표기 ─▶ 소문자화 + 공백/특수문자 제거 ─▶ config.yaml: company_aliases 조회
                                                          │
                              ┌───────────────────────────┴────────────────────┐
                          매칭 성공                                        매칭 실패
                              │                                                │
                  canonical = 대표명                              canonical = 원문표기 그대로
                  resolved = true                                 resolved = false
                                                                  + observability: unknown_company
```

`config.yaml` 형식 (대표명 → alias 목록):

```yaml
company_aliases:
  OpenAI: ["openai", "open ai", "오픈AI", "오픈에이아이"]
```

**원문표기를 버리지 않는 이유**: 정규화가 틀렸을 때(예: 동명이인 기관) 되짚을
근거가 남고, `unknown_company` 로그가 alias 목록을 늘리는 작업 큐가 된다.
alias 목록을 코드가 아니라 `config.yaml` 에 둔 것도 같은 이유 — 이건 코드 변경이
아니라 데이터 갱신이다.

| 하위 필드 | 한국어 키 | 설명 |
|---|---|---|
| `raw` | `원문표기` | 본문에 나온 그대로 |
| `canonical` | `정규명` | 정규화된 대표명 |
| `resolved` | `정규화성공` | alias 매칭 성공 여부 |
| `role` | `역할` | 이 소식에서의 역할 (`발표 주체`, `인수 대상`, `경쟁사` 등). 선택 |

## 4. 관련기존기술 (`prior_art`) — 자유 태그 (영어 표준형)

이 소식이 **딛고 선** 기존 기술/개념. 0~8개.
예: `Transformer`, `MoE`, `FlashAttention`, `DPO`, `RLHF`, `Vector DB`

### 표기 규칙: 영어 표준형으로 통일한다

기업명과 달리 이 필드에는 **정규화 사전이 없다.** 사전이 없는 채로 표기가 갈리면
같은 개념이 Obsidian 그래프에서 여러 노드로 쪼개진다. 첫 추출 테스트에서 한 기사
안에서만 표기가 세 갈래로 나뉘는 것이 확인됐다. (결정 로그 D-020)

| 규칙 | O | X |
|---|---|---|
| 영어 표준형만 쓴다 | `Fuzzing` | `퍼징`, `퍼징(Fuzzing)` |
| 병기하지 않는다 | `Vibe Coding` | `바이브코딩`, `바이브코딩(Vibe Coding)` |
| 한국어 기사여도 영어로 | `Transformer` | `트랜스포머` |

- **병기는 세 번째 변종을 만든다.** 괄호 병기를 허용하면 `Fuzzing`, `퍼징`,
  `퍼징(Fuzzing)` 이 모두 유효해져 사전 없이는 통합이 불가능해진다.
- 널리 쓰이는 영어 표준형이 **없는 한국어 고유 개념**일 때만 한국어로 적는다.
  이 경우는 드물다. 영어 표기가 떠오르지 않으면 그 태그를 빼는 쪽을 택한다.

**사전이 아니라 규칙으로 정한 이유:** 기업명은 후보가 유한하고 자주 반복돼
alias 사전이 유지 가능하다. 반면 기존기술 태그는 어휘 규모를 아직 모른다.
규모를 모르는 상태에서 사전을 먼저 만들면 틀린 사전이 되고, 사전 유지 비용이
개인 프로젝트에서 감당이 안 된다. 생성 시점에 표기를 맞추는 쪽이 싸다.

### 내용 규칙
- 새 기술이 아니라 **기반이 된** 것을 적는다. "무엇의 다음 단계인가"를 기록하는 필드.
- 시간이 지나면 그래도 파편화되므로, 분기마다 상위 빈도 태그를 검토해 통합 필요성을
  살핀다. 그때 실제 어휘 규모를 보고 사전 도입 여부를 다시 판단한다.

## 5. 영향도 (`impact`) — 1~5 리커트 + 근거

가장 주관적인 필드. 그래서 **근거 한 줄을 필수**로 받는다(15자 이상).

| 점수 | 기준 | 예시 |
|---|---|---|
| 1 | 점진적·국소적. 특정 제품의 소소한 업데이트 | 마이너 버전 릴리스 |
| 2 | 특정 도메인 종사자만 관심 | 특정 프레임워크의 성능 개선 |
| 3 | 업계 일반이 주목. 다수 팀의 선택지에 영향 | 주요 벤더의 신규 API |
| 4 | 판도 변화 가능. 기존 접근을 재검토하게 만듦 | 비용 구조를 바꾸는 모델 출시 |
| 5 | 패러다임 전환. 이후 연구·제품의 전제가 바뀜 | 트랜스포머 논문급 |

- 5는 아껴 쓴다. 1년에 몇 건 수준이어야 척도가 의미를 가진다.
- 근거는 **왜 그 점수인지**를 쓴다. 기사 요약을 반복하지 않는다.
- eval 은 이 점수를 골든셋과 **quadratic weighted kappa** 로 비교한다.
  ±1은 사실상 허용, ±3은 크게 감점 — 순서형 척도이므로 단순 정확도는 부적절하다.

---

## Obsidian frontmatter 매핑

`NewsNote.model_dump(by_alias=True)` 결과를 아래처럼 평평하게 펴서 쓴다.

```yaml
---
제목: "..."
기술영역: [LLM, Reasoning]      # 리스트
발표유형: ProductLaunch          # 단일 값
관련기업: [Anthropic]            # canonical 만 (원문표기는 본문 각주로)
관련기존기술: [Transformer, RLHF]
영향도: 4                        # ← Impact.score
영향도근거: "..."                # ← Impact.rationale
출처: https://...
발행일: 2026-08-29
---
```

`영향도`를 중첩 객체가 아니라 두 개의 평평한 키로 펴는 이유는 Obsidian 의
Properties/Dataview 가 중첩 객체를 정렬·필터 대상으로 잘 다루지 못하기 때문이다.
코드 안에서는 `Impact` 객체를 유지하고, 직렬화 시점에만 편다.

---

## 열린 질문

- [ ] `기술영역` 상한 3개가 적절한가? 골든셋 30건 라벨링 후 재검토.
- [ ] `발표유형`에 `Incident/Outage`(장애·사고)를 추가할 것인가?
- [ ] `관련기존기술`에도 alias 사전이 필요해지는 시점은 언제인가?
- [ ] 영향도를 "기술적 영향"과 "시장 영향"으로 분리할 것인가? (지금은 합쳐서 1개)
